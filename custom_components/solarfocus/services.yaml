enable_cooling:
  name: Enable Cooling
  description: Request heat pump to start cooling
  fields:
    enable:
      name: Enable
      description: 'True = Cooling enabled'
      required: true
      example: true
      selector:
        boolean:

